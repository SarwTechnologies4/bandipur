/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['foodapp.siddharthahospitality.com'],
    // If you need to use other domains, add them like this:
    // domains: ['foodapp.siddharthahospitality.com', 'another-domain.com'],
  },

};

export default nextConfig;
